# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# This machine's global core.excludesfile ignores package-lock.json. Without the
# lockfile `npm ci` cannot run, which is the documented install step here (and the
# one that matters for a second machine), so it is tracked despite that rule.
!package-lock.json
